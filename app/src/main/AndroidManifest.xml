<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.WeCycle" >
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <queries>
        <package android:name="com.google.android.apps.maps" />
    </queries>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartRecycle" >
        <activity
            android:name="com.example.WeCycle.Coupons"
            android:exported="false" />
        <activity
            android:name="com.example.WeCycle.Top_garbage_collectors"
            android:exported="false" />
        <activity
            android:name="com.example.WeCycle.QRCodeScanner"
            android:exported="false" />
        <activity
            android:name="com.example.WeCycle.AfterLoginMenu"
            android:exported="false" />
        <activity
            android:name="com.example.WeCycle.MapsActivity"
            android:exported="false"
            android:label="@string/title_activity_maps"
            android:theme="@style/Theme.SmartRecycle.NoActionBar" />
        <activity
            android:name="com.example.WeCycle.PassFG"
            android:exported="false" />
        <activity
            android:name="com.example.WeCycle.Succes"
            android:exported="false" />
        <activity
            android:name="com.example.WeCycle.Register"
            android:exported="false" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name="com.example.WeCycle.Login"
            android:exported="false" />
        <activity
            android:name="com.example.WeCycle.MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>